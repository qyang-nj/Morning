<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:actionBarStyle">@style/Custom.ActionBar</item>
        <!-- Intentionally hide the actionbar at the very beginning.  -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowTitleSize">0dp</item>

        <item name="android:editTextBackground">@drawable/apptheme_edit_text_holo_light</item>
        <item name="android:listChoiceIndicatorMultiple">@drawable/apptheme_btn_check_holo_light</item>
        <item name="android:alertDialogTheme">@style/Custom.AlertDialog</item>

        <item name="asb_switchStyle">@style/Custom.Switch</item>
        <item name="asb_switchPreferenceStyle">@style/Preference.SwitchPreference</item>
    </style>

    <style name="Custom.ActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/main_color</item>
        <item name="android:displayOptions">showTitle</item>
        <item name="android:titleTextStyle">@style/Custom.ActionBar.TitleTextStyle</item>
    </style>

    <style name="Custom.ActionBar.TitleTextStyle" parent="@android:style/Widget.TextView">
        <item name="android:textSize">36sp</item>
        <item name="android:textColor">@color/dark_grey</item>
    </style>

    <style name="Custom.Switch" parent="@style/Widget.Holo.Light.CompoundButton.Switch" >
        <item name="asb_track">@drawable/apptheme_switch_track_holo_light</item>
        <item name="asb_thumb">@drawable/apptheme_switch_inner_holo_light</item>
    </style>

    <style name="Custom.AlertDialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:listChoiceIndicatorMultiple">@drawable/apptheme_btn_check_holo_light</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowMinWidthMajor">@android:dimen/dialog_min_width_major</item>
        <item name="android:windowMinWidthMinor">@android:dimen/dialog_min_width_minor</item>
    </style>
</resources>
